% \usetikzlibrary{calc}

\newcommand{\wire}[5]{
    \def\color{#5}
    % \coordinate (P1) at (#1,#2);
    % \coordinate (P2) at (#3,#4);

    % \draw[line width=1mm, color=gray, line cap=round] (#1,#2) -- (#3,#4);
    \draw[line width=2mm, color=\color, line cap=round] (#1,#2) -- (#3,#4);

    % \draw[line width=2mm, color=\color, line cap=round]
    % let
    %   \p1 = ($(P2)-(P1)$),
    %   \n1 = {atan2(\y1,\x1)},
    %   \n2 = {veclen(\x1,\y1)},
    % in
    % [rotate=\n1]
    % ($(P1)!0.15!(P2)$) -- ++(\n2 - 12,0);
}